#include "main.h"
/**
  * _abs - calculates absolute values of numbers
  *
  * @n: an integer
  *
  * Return: absolute values
  *
  */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
